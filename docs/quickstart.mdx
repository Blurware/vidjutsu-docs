---
title: "Quickstart"
description: "Get your API key and create your first draft account in minutes"
---

## Option A: Use the CLI

The fastest way to get started. Install the CLI and authenticate in one step.

```bash
curl -fsSL https://vidjutsu.ai/install.sh | bash
```

Then authenticate with your card:

```bash
vidjutsu auth --card tok_live_your_card_token
```

Create a draft account (free):

```bash
vidjutsu account create --platform instagram --name "my_brand" --niche "fitness"
```

Provision when ready (990 credits):

```bash
vidjutsu account provision acc_abc123 --niche "fitness" --country US
```

See the [CLI reference](/cli/index) for all commands.

---

## Option B: Use the API directly

### 1. Top up credits

Start a Stripe checkout session. Pay with any credit or debit card.

```bash
curl -X POST https://api.vidjutsu.ai/v1/api_keys \
  -H "Content-Type: application/json" \
  -d '{
    "credits": 100,
    "email": "you@example.com"
  }'
```

```json
{
  "url": "https://checkout.stripe.com/...",
  "sessionId": "cs_...",
  "credits": 100,
  "amount": 1000
}
```

Open the returned URL in your browser to complete payment.

### 2. Get your API key

After payment, poll the status endpoint:

```bash
curl "https://api.vidjutsu.ai/v1/api_keys/status?session=cs_..."
```

```json
{
  "status": "completed",
  "apiKey": "vj_live_abc123...",
  "clientId": "...",
  "balance": 100
}
```

<Warning>
Store your API key securely — it is shown once in this response.
</Warning>

### 3. Create a draft account (free)

```bash
curl -X POST https://api.vidjutsu.ai/v1/accounts \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: vj_live_abc123..." \
  -d '{
    "platform": "instagram",
    "name": "my_brand",
    "niche": "fitness",
    "tags": [
      { "key": "campaign", "value": "my-first" }
    ]
  }'
```

```json
{
  "id": "acc_abc123",
  "status": "draft",
  "creditsCharged": 0
}
```

### 4. Provision the account (990 credits)

```bash
curl -X POST https://api.vidjutsu.ai/v1/accounts/provision \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: vj_live_abc123..." \
  -d '{
    "accountId": "acc_abc123",
    "country": "US",
    "niche": "fitness"
  }'
```

```json
{
  "id": "acc_abc123",
  "status": "creating",
  "creditsCharged": 990
}
```

### 5. Create a draft post (free)

```bash
curl -X POST https://api.vidjutsu.ai/v1/posts \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: vj_live_abc123..." \
  -d '{
    "accountId": "acc_abc123",
    "videoUrl": "https://your-video-url.com/video.mp4",
    "caption": "Your caption here"
  }'
```

```json
{
  "id": "post_xyz789",
  "status": "draft",
  "creditsCharged": 0
}
```

### 6. Schedule the post (36 credits)

Once the account is active:

```bash
curl -X POST https://api.vidjutsu.ai/v1/posts/schedule \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: vj_live_abc123..." \
  -d '{ "postId": "post_xyz789" }'
```

## Next steps

<CardGroup cols={2}>
  <Card title="Manage accounts" icon="user-plus" href="/guides/manage-accounts">
    Learn the full draft-first account workflow.
  </Card>
  <Card title="CLI reference" icon="terminal" href="/cli/index">
    Full command reference for the VidJutsu CLI.
  </Card>
</CardGroup>
