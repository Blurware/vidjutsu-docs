---
title: "Quickstart"
description: "Get your API key and run your first watch, extract, and transcribe calls in minutes"
---

## Option A: Use the CLI

The fastest way to get started. Install the CLI and authenticate in one step.

```bash
curl -fsSL https://vidjutsu.ai/install.sh | bash
```

Then authenticate with your API key:

```bash
vidjutsu auth --key vj_live_your_api_key
```

Watch a video:

```bash
vidjutsu watch --video-url "https://cdn.example.com/video.mp4" --prompt "Is the hook effective?"
```

See the [CLI reference](/cli/index) for all commands.

---

## Option B: Use the API directly

### 1. Top up credits

Start a Stripe checkout session. Pay with any credit or debit card.

```bash
curl -X POST https://api.vidjutsu.ai/v1/credits \
  -H "Content-Type: application/json" \
  -d '{
    "credits": 100,
    "email": "you@example.com"
  }'
```

```json
{
  "url": "https://checkout.stripe.com/...",
  "sessionId": "cs_...",
  "credits": 100,
  "amount": 1000
}
```

Open the returned URL in your browser to complete payment.

### 2. Get your API key

After payment, poll the status endpoint:

```bash
curl "https://api.vidjutsu.ai/v1/credits/status?session=cs_..."
```

```json
{
  "status": "completed",
  "apiKey": "vj_live_abc123...",
  "clientId": "...",
  "balance": 100
}
```

<Warning>
Store your API key securely — it is shown once in this response.
</Warning>

### 3. Watch a video (10 credits)

```bash
curl -X POST https://api.vidjutsu.ai/v1/watch \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer vj_live_abc123..." \
  -d '{
    "mediaUrl": "https://cdn.example.com/video.mp4",
    "prompt": "Is the hook strong enough for fitness TikTok?"
  }'
```

```json
{
  "response": "The hook lands in the first 1.2 seconds — direct address to camera with a bold claim. Strong for fitness TikTok. Consider adding a text overlay to reinforce for silent viewers."
}
```

### 4. Extract frames and audio (5 credits)

```bash
curl -X POST https://api.vidjutsu.ai/v1/extract \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer vj_live_abc123..." \
  -d '{
    "mediaUrl": "https://cdn.example.com/video.mp4",
    "frames": "auto",
    "audio": true
  }'
```

```json
{
  "frames": [{ "index": 0, "url": "https://cdn.vidjutsu.ai/..." }],
  "audio": { "url": "https://cdn.vidjutsu.ai/...", "duration": 10.2 },
  "metadata": { "width": 1080, "height": 1920, "fps": 30, "duration": 10.0 }
}
```

### 5. Transcribe speech (10 credits)

```bash
curl -X POST https://api.vidjutsu.ai/v1/transcribe \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer vj_live_abc123..." \
  -d '{
    "mediaUrl": "https://cdn.example.com/video.mp4"
  }'
```

```json
{
  "transcript": "the one thing nobody tells you about creatine",
  "words": [{ "word": "the", "start": 0.1, "end": 0.2 }],
  "language": "en",
  "duration": 10.2
}
```

## Next steps

<CardGroup cols={2}>
  <Card title="API Reference" icon="code" href="/api-reference/introduction">
    Full endpoint reference for watch, extract, transcribe, check, and storage.
  </Card>
  <Card title="CLI reference" icon="terminal" href="/cli/index">
    Full command reference for the VidJutsu CLI.
  </Card>
</CardGroup>
