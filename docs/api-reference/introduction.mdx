---
title: "API Reference"
description: "Complete reference for the VidJutsu Video Intelligence API"
---

## Base URL

```
https://api.vidjutsu.ai
```

## Authentication

Most endpoints require an API key passed in the `X-Api-Key` header:

```bash
curl https://api.vidjutsu.ai/v1/accounts \
  -H "X-Api-Key: vj_live_your_key_here"
```

Get your API key by [purchasing credits](/authentication) with any card via Stripe.

## Endpoints

### Intelligence

| Method | Endpoint | Description | Credits |
|--------|----------|-------------|---------|
| POST | `/v1/critic` | QA gate — pass/fail verdict with issues | 10 |
| POST | `/v1/breakdown` | Start a deep video analysis (async) | 10 |
| GET | `/v1/breakdown` | Poll for breakdown result | 0 |
| POST | `/v1/score` | Viral potential prediction | 10 |

### Storage

| Method | Endpoint | Description | Credits |
|--------|----------|-------------|---------|
| POST | `/v1/accounts` | Create an account record | 0 |
| PUT | `/v1/accounts` | Update an account record | 0 |
| GET | `/v1/accounts` | List accounts (supports tag filtering) | 0 |
| DELETE | `/v1/accounts` | Delete an account | 0 |
| POST | `/v1/posts` | Create a post record | 0 |
| PUT | `/v1/posts` | Update a post record | 0 |
| GET | `/v1/posts` | List posts (supports tag filtering) | 0 |
| DELETE | `/v1/posts` | Delete a post | 0 |
| POST | `/v1/upload` | Upload media to CDN | 0 |
| POST | `/v1/upload/url` | Get a signed upload URL | 0 |

### Billing

| Method | Endpoint | Description | Credits |
|--------|----------|-------------|---------|
| POST | `/v1/api_keys` | Start a Stripe checkout session | 0 |
| GET | `/v1/api_keys/status` | Check payment status / get API key | 0 |
| POST | `/v1/api_keys/rotate` | Rotate your API key | 0 |
| POST | `/v1/api_keys/recover` | Recover a lost API key | 0 |
| GET | `/v1/balance` | Check credit balance | 0 |
| GET | `/v1/pricing` | Get current pricing table | 0 |

## Tags

Accounts and posts support AWS-style key-value tags for organization and filtering:

```json
{
  "tags": [
    { "key": "brand", "value": "summer-launch" },
    { "key": "region", "value": "northeast" }
  ]
}
```

Filter on GET requests: `?tag.brand=summer-launch`

## Response codes

| Code | Description |
|------|-------------|
| `200` | Success (read operations, edits) |
| `201` | Created (new records) |
| `202` | Accepted (async operation started — breakdown) |
| `400` | Missing or invalid parameters |
| `401` | Missing or invalid API key |
| `402` | Insufficient credits |

## Error format

All errors return a consistent JSON structure:

```json
{
  "error": "insufficient_credits",
  "message": "This operation requires 10 credits but you have 2",
  "setup": "/v1/api_keys"
}
```

The `setup` field is included on `402` errors to help agents self-recover by initiating a top-up.

## Async operations

Breakdown analysis returns `202 Accepted`:

```json
{
  "id": "va_abc123",
  "status": "processing"
}
```

Poll `GET /v1/breakdown?id=va_abc123` until `status` changes to `"completed"`.

## CORS

The API allows cross-origin requests from any origin. Allowed headers: `Content-Type`, `X-Api-Key`, `Authorization`.

## Versioning

Responses include the `VidJutsu-Version` header (`2026-03-25`) indicating the current API version.

## Rate limits

No rate limits are enforced during beta. Fair use applies.
